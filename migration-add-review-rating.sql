-- =====================================================
-- مهاجرت: افزودن امتیاز ستاره‌ای (۱ تا ۵) به دیدگاه‌های مشتریان
-- این فایل را در تب Console دیتابیس shamkar-db روی Cloudflare Dashboard اجرا کن
-- =====================================================

ALTER TABLE reviews ADD COLUMN rating INTEGER;
