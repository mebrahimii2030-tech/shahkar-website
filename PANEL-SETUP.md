# راهنمای راه‌اندازی پنل مشتریان روی Cloudflare Workers

با بررسی دقیق دیدیم پروژه‌ی `shahkar-website` روی داشبورد Cloudflare از نوع **Worker** است (نه Pages) و به ریپازیتوری گیت‌هاب `mebrahimii2030-tech/shahkar-website` وصل است. این راهنما مخصوص همین حالت است.

## چیزهایی که باید حذف/جایگزین کنی

- پوشه‌ی قدیمی `functions/` (اگر قبلاً اضافه کرده بودی) را کامل **حذف کن** — آن ساختار فقط برای Cloudflare Pages کار می‌کند، نه Workers، و اگر بماند هم استفاده نمی‌شود و هم سورس‌کدش به‌صورت عمومی قابل دانلود می‌شود.
- اگر قبلاً یک `wrangler.jsonc` دیگری (مثلاً با فیلد `pages_build_output_dir`) ساخته بودی، با فایل جدید `wrangler.jsonc` که اینجا هست جایگزینش کن.

## فایل‌هایی که باید در ریشه پروژه قرار بگیرند

- `wrangler.jsonc` — تنظیمات اصلی Worker (نام پروژه، اتصال به D1، مسیر فایل استاتیک)
- `.assetsignore` — جلوگیری از عمومی‌شدن فایل‌های سورس (`src/`, `schema.sql`, همین راهنما و ...)
- `src/worker.js` — کل منطق سرور (API مشتریان/خودروها/مراجعات/قطعات + محافظت پنل مدیریت)، جایگزین پوشه قدیمی `functions/`
- بقیه فایل‌ها (`schema.sql`, `panel-admin.html`, `panel-customer.html`, `customer.html`, `css/panel.css`, `js/panel-*.js`, `js/combobox.js`, `js/jalali.js`, `js/customer-view.js`, `js/panel-data.js`) دقیقاً مثل قبل، کنار فایل‌های فعلی سایتت.

## مرحله ۱: ساخت پایگاه‌داده D1 (اگر هنوز نساختی)

از قبل با چت‌جی‌پی‌تی این کار انجام شده: دیتابیس `shamkar-db` با شناسه‌ی
`b3e72150-6858-4a63-ba8d-957a8808af01` ساخته شده و جدول‌ها هم داخلش هستند.
اگر جدول‌ها را نساختی، محتوای `schema.sql` را در تب **Console** همان دیتابیس (روی Cloudflare Dashboard → Workers & Pages → D1 → shamkar-db) اجرا کن.

اگر شناسه دیتابیس‌ات چیز دیگری بود (نه همین که بالا نوشته شد)، توی `wrangler.jsonc` مقدار `database_id` را با شناسه‌ی درستت جایگزین کن — از دستور `npx wrangler d1 list` یا از داشبورد D1 می‌تونی ببینیش.

## مرحله ۲: تنظیم نام کاربری و رمز عبور پنل مدیریت

این را حتماً از **داشبورد** انجام بده، نه از فایل کد (چون این مقادیر نباید داخل گیت‌هاب و به‌صورت عمومی ذخیره بشن):

۱. برو به پروژه `shahkar-website` در Cloudflare
۲. **Settings → Variables and Secrets**
۳. دو متغیر جدید بساز (هر دو نوعشان **Secret** باشد):
   - نام `ADMIN_USERNAME`، مقدار یک نام کاربری دلخواه (مثلاً همان که خودت انتخاب می‌کنی)
   - نام `ADMIN_PASSWORD`، مقدار یک رمز قوی
۴. ذخیره کن

این مقادیر مستقل از گیت‌هاب هستند و با هر بار `push` از بین نمی‌روند. اگر فقط `ADMIN_PASSWORD` را تنظیم کنی و `ADMIN_USERNAME` را نساخته باشی، صفحه‌ی ورود پنل غیرفعال می‌ماند و پیام «پیکربندی نشده است» نشان می‌دهد.

### قابلیت‌های امنیتی پنل مدیریت

- **خروج خودکار پس از عدم فعالیت:** اگر پنل به مدت ۲۰ دقیقه بدون هیچ فعالیتی (کلیک، اسکرول، تایپ) باز بماند، ۶۰ ثانیه قبل از خروج یک هشدار نشان داده می‌شود و در صورت عدم واکنش، نشست به‌صورت خودکار بسته و کاربر به صفحه‌ی ورود برمی‌گردد.
- **ثبت زمان آخرین ورود:** هر بار ورود موفق به پنل (به همراه زمان و آدرس اینترنتی) در جدول `admin_logins` ثبت می‌شود؛ زمان ورود قبلی بالای داشبورد نمایش داده می‌شود.
- **هشدار ورود مشکوک:** اگر آدرس اینترنتی ورود فعلی با آخرین ورود موفق قبلی فرق داشته باشد، یک بنر هشدار قرمز بالای داشبورد نشان داده می‌شود.

برای فعال شدن این قابلیت‌ها روی دیتابیس فعلی (Production)، حتماً فایل `migration-add-admin-security.sql` را یک‌بار در تب **Console** دیتابیس `shamkar-db` روی Cloudflare Dashboard اجرا کن (نه `schema.sql` کامل، چون آن فایل جدول‌ها را پاک می‌کند).

## مرحله ۳: انتشار

فایل‌ها را به ریپازیتوری `mebrahimii2030-tech/shahkar-website` اضافه کن و:

```
git add .
git commit -m "افزودن پنل مشتریان (سازگار با Cloudflare Workers)"
git push
```

چون پروژه به گیت‌هاب وصل است، Cloudflare خودش build و deploy می‌کند (همان چیزی که در تصویر داشبورد به‌عنوان build minutes دیده می‌شود).

## اگر دستی هم خواستی تست کنی

```
npx wrangler deploy
```

این دستور را از داخل پوشه‌ی پروژه (همان‌جایی که `wrangler.jsonc` هست) اجرا کن. اگر پیام خطا داد، متن کامل خطا را بفرست تا دقیق بررسی کنیم.

## آدرس‌های پنل بعد از انتشار

- **پنل مدیریت (فقط خودت، با رمز عبور):** `https://shahkar-website.m-ebrahimii2030.workers.dev/panel-admin.html` (یا دامنه اختصاصی‌ات اگر وصل کردی)
- **پنل مدیریت وبلاگ (فقط خودت، با همان رمز عبور):** `https://shahkar-website.m-ebrahimii2030.workers.dev/panel-blog.html`
- **صفحه اختصاصی هر مشتری (بدون رمز، فقط با لینک):** `.../customer.html?code=XXXXXXXXXX`

## پنل مدیریت وبلاگ (افزودن/ویرایش/حذف مقاله بدون کدنویسی)

یک جدول جدید به نام `articles` و یک پنل مدیریت اختصاصی به نام `panel-blog.html` اضافه شده که با آن می‌توانی مقالات وبلاگ سایت را کامل مدیریت کنی، بدون نیاز به دست‌زدن به کد.

### قدم ۱: اجرای مهاجرت دیتابیس

فایل `migration-add-blog.sql` را در تب **Console** دیتابیس `shamkar-db` روی Cloudflare Dashboard اجرا کن (نه `schema.sql`، چون آن فایل همه‌ی جدول‌ها را پاک می‌کند). این فایل:

- جدول `articles` را می‌سازد.
- همان ۱۰ مطلبی که الان به‌صورت ثابت در `blog.html` نوشته شده بودند را به‌عنوان داده اولیه در دیتابیس ثبت می‌کند، تا از همان روز اول در پنل مدیریت وبلاگ قابل ویرایش یا حذف باشند.

این فایل با `INSERT OR IGNORE` نوشته شده، پس اگر یک‌بار اشتباهی دوباره اجرایش کنی، مشکلی پیش نمی‌آید و رکورد تکراری ساخته نمی‌شود.

### قدم ۲: انتشار فایل‌های جدید

فایل‌های زیر باید کنار بقیه فایل‌های سایت در ریشه پروژه/ریپازیتوری قرار بگیرند (`git add .` و `git push` مثل قبل):

- `panel-blog.html`, `js/panel-blog.js`, `css/panel-blog.css`
- نسخه‌ی به‌روزشده‌ی `src/worker.js`, `js/panel-api.js`, `wrangler.jsonc`
- نسخه‌ی به‌روزشده‌ی `blog.html` و `js/blog.js` (حالا این‌ها دیگر مطالب را از دیتابیس می‌خوانند، نه از کد ثابت)

### قدم ۳: استفاده از پنل

از پنل مدیریت وبلاگ می‌توانی:

- مقاله جدید اضافه کنی (عنوان، خلاصه، متن کامل، دسته‌بندی، آیکون، نویسنده، زمان مطالعه، تاریخ انتشار شمسی).
- هر مقاله را ویرایش یا کامل حذف کنی.
- یک مقاله را «پیش‌نویس» نگه داری (فقط در پنل دیده می‌شود، در سایت نمایش داده نمی‌شود) یا آن را به‌عنوان «مطلب ویژه» بالای صفحه وبلاگ نمایش بدهی.
- دسته‌بندی‌ها را آزادانه هر اسمی که بخواهی تایپ کنی؛ سایدبار «دسته‌بندی‌ها»ی صفحه وبلاگ خودش خودکار از روی همین مقادیر ساخته می‌شود، نیازی به تعریف جداگانه نیست.

نکته: چون هنوز آماری از تعداد بازدید واقعی مقالات در دیتابیس ذخیره نمی‌شود، بخش «پرمخاطب‌ترین» قبلی در صفحه وبلاگ به «جدیدترین مطالب» تغییر کرده (۴ مقاله‌ی اخیر بر اساس تاریخ انتشار).

## نکته درباره تاریخ‌ها

همه تاریخ‌ها (تاریخ مراجعه، موعد تعویض قطعه) به‌صورت **شمسی** وارد و نمایش داده می‌شوند (مثلاً `۱۴۰۵/۰۴/۲۸`)؛ تبدیل به میلادی فقط پشت صحنه برای ذخیره‌سازی انجام می‌شود.

## نکته درباره Encoding فارسی

اگر فایل‌ها را با ادیتوری مثل Notepad ساده در ویندوز باز/ذخیره کردی و کامنت‌های فارسی به‌هم‌ریخته دیدی، هنگام ذخیره باید Encoding را روی **UTF-8** بگذاری (نه ANSI). در VS Code این تنظیم پایین صفحه (گوشه راست پایین) قابل تغییر است. فایل‌هایی که من اینجا تحویل دادم همه از ابتدا UTF-8 هستند؛ این نکته فقط برای زمانی‌ست که خودت فایلی را در ویندوز ویرایش و دوباره ذخیره می‌کنی.
