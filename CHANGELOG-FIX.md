# تغییرات این نسخه (رفع باگ‌ها)

## ۱) رفع باگ «ثبت نشدن مراجعه»
فایل: `js/jalali.js`
علت: تابع `jalaliInputToIso` تاریخ ورودی را با `Number()` می‌خواند که فقط ارقام
انگلیسی را می‌فهمد. اگر مشتری/مکانیک تاریخ را با ارقام فارسی وارد می‌کرد
(مثلاً از صفحه‌کلید گوشی)، تبدیل شکست می‌خورد، مقدار `null` برمی‌گشت، فرم با
پیام خطا رد می‌شد و مراجعه اصلاً در دیتابیس ذخیره نمی‌شد.
راه‌حل: قبل از تبدیل، ارقام فارسی/عربی به انگلیسی نرمال می‌شوند (با تابع
موجود `normalizeDigits`). تست شد و هر دو حالت فارسی و انگلیسی درست کار می‌کنند.

## ۲) دکمه ویرایش مشتری در پنل مدیریت
فایل‌ها: `panel-admin.html`, `js/panel-admin.js`
API بروزرسانی مشتری (`PUT /api/customers/:code`) از قبل در `src/worker.js`
وجود داشت، ولی جدول اصلی پنل مدیریت دکمه‌ای برای استفاده از آن نداشت (فقط از
داخل صفحه اختصاصی هر مشتری قابل ویرایش بود). حالا یک دکمه «ویرایش» در ستون
عملیات هر ردیف اضافه شده که یک مودال باز می‌کند و مستقیماً از همان جدول اصلی
قابل استفاده است.

## ۳) حذف کامل منطق موعد بر اساس تاریخ؛ فقط کارکرد (کیلومتر)
فایل‌ها: `schema.sql`, `migration-add-mileage.sql`, `src/worker.js`,
`js/jalali.js`, `js/panel-admin.js`, `js/panel-customer.js`, `js/customer-view.js`,
`panel-customer.html`, `customer.html`

- ستون `next_due_date` از جدول `parts_replaced` حذف شد.
- ستون جدید `current_mileage` به جدول `cars` اضافه شد (آخرین کارکرد ثبت‌شده خودرو).
- فرم «ثبت مراجعه» حالا یک فیلد «کارکرد فعلی خودرو» دارد (الزامی) که با هر
  مراجعه، کارکرد خودرو را به‌روزرسانی می‌کند.
- وضعیت موعد (گذشته/نزدیک/عادی) دیگر بر اساس تاریخ شمسی محاسبه نمی‌شود؛
  بلکه از تفاضل «کارکرد موعد بعدی قطعه» و «کارکرد فعلی خودرو» به دست می‌آید:
  - `remaining <= 0` → موعد گذشته
  - `0 < remaining <= 1500` کیلومتر → نزدیک است
  - در غیر این صورت → بدون نگرانی
  این منطق در یک تابع مشترک `dueStatusByMileage` در `js/jalali.js` قرار گرفته
  و در پنل مدیریت، پرونده مشتری، و صفحه عمومی مشتری استفاده می‌شود.
- **مهم:** اگر روی دیتابیس فعلی (Production) این آپدیت را می‌گذاری، حتماً
  `migration-add-mileage.sql` را در کنسول D1 اجرا کن (نه `schema.sql` کامل،
  چون آن فایل جدول‌ها را پاک می‌کند). توضیحات کامل داخل همان فایل هست.

## ۴) امنیت ورود پنل مدیریت: یوزرنیم، خروج خودکار، ثبت ورود، هشدار ورود مشکوک
فایل‌ها: `src/worker.js`, `panel-login.html`, `panel-admin.html`,
`panel-customers.html`, `panel-customer.html`, `panel-blog.html`,
`js/panel-idle-logout.js` (جدید), `js/panel-security-banner.js` (جدید),
`css/panel.css`, `schema.sql`, `migration-add-admin-security.sql` (جدید)

- **نام کاربری:** ورود پنل دیگر فقط با رمز عبور نیست؛ حالا یک فیلد «نام
  کاربری» هم دارد. مقدار آن از متغیر `ADMIN_USERNAME` (کنار `ADMIN_PASSWORD`
  موجود) در Settings پروژه Worker روی Cloudflare خوانده می‌شود.
- **خروج خودکار پس از عدم فعالیت:** یک تایمر ۲۰ دقیقه‌ای در تمام صفحات پنل
  مدیریت فعال است؛ ۶۰ ثانیه قبل از پایان، هشداری با شمارش معکوس نشان داده
  می‌شود و در صورت عدم تعامل، نشست بسته و کاربر به صفحه‌ی ورود هدایت می‌شود
  (با پیام توضیح دلیل). این تایمر بین چند تب باز هم‌زمان است.
- **ثبت زمان آخرین ورود:** هر تلاش ورود (موفق/ناموفق) در جدول جدید
  `admin_logins` با زمان، آدرس اینترنتی و مرورگر ثبت می‌شود. بالای داشبورد
  (`panel-admin.html`) زمان آخرین ورود موفق نمایش داده می‌شود.
- **هشدار ورود مشکوک:** اگر آدرس اینترنتی ورود فعلی با آخرین ورود موفق قبلی
  فرق داشته باشد، یک بنر قرمز بالای داشبورد نشان می‌دهد که این ورود از
  موقعیت جدیدی انجام شده است.
- **مهم:** اگر روی دیتابیس فعلی (Production) این آپدیت را می‌گذاری، حتماً
  `migration-add-admin-security.sql` را در کنسول D1 اجرا کن، و حتماً متغیر
  `ADMIN_USERNAME` را هم در Settings پروژه Worker تعریف کن؛ وگرنه صفحه‌ی
  ورود پنل غیرفعال می‌ماند تا این تنظیم انجام شود.

## ۵) تست‌های انجام‌شده در این محیط
- تمام فایل‌های جاوااسکریپت تغییر‌یافته از نظر نحوی (syntax) بررسی شدند.
- تابع `jalaliInputToIso` با ورودی ارقام فارسی و انگلیسی تست شد (هر دو درست کار می‌کنند).
- تابع `dueStatusByMileage` برای هر ۵ حالت (بدون موعد، کارکرد نامشخص، گذشته،
  نزدیک، عادی) تست شد.
- **تست‌هایی که فقط روی Cloudflare واقعی قابل انجام‌اند** (چون این محیط به
  D1/Workers واقعی متصل نیست) و باید خودت بعد از انتشار انجام بدی:
  ثبت مشتری جدید، ویرایش مشتری از دکمه جدید، ثبت مراجعه با کارکرد فعلی،
  نمایش سوابق در پرونده مشتری و صفحه عمومی، و درست‌بودن رنگ/متن هشدار موعد.
